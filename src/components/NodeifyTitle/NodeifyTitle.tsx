'use client';

import React from 'react';
import styles from './NodeifyTitle.module.css';

export const NodeifyTitle = () => {
    return (
        <div className={styles.container}>
            <h1 className={styles.title}>Nodeify</h1>
        </div>
    );
};
